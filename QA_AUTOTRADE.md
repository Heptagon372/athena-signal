# 자동매매 QA 심사보고서

**대상** `engine/autotrade.py` · `engine/risk.py` · `engine/broker.py` · `engine/strategy.py` · `storage/autotrade.py` · `api.py`
**일시** 2026-08-08 KST
**방법** 코드 심사 + 격리 DB(임시 파일)에서 재현 테스트. 실계좌 `athena.db` 는 건드리지 않았습니다.
**판정** **불합격 — 실계좌(`live`) 가동 금지**

> 이 문서는 **심사 시점의 기록**입니다. 수정 진행 상황은 [QA_MEMO.md](QA_MEMO.md) 에서
> 관리합니다. 2026-08-08 현재 **1번(치명) 수정 완료**, 나머지 9건은 그대로입니다.
> 2·3·4번이 남아 있어 `live` 가동 금지 판정은 유지됩니다.

---

## 1. 요약

발견한 결함 10건 중 **4건이 치명**이고, 그 4건은 전부 `paper` 에서는 정상 동작하고
**`live`·`mock` 에서만 깨집니다.** 즉 모의계좌로 며칠 지켜보는 권장 절차로는
절대 드러나지 않습니다.

| # | 결함 | 등급 | paper | live/mock |
|---|---|---|---|---|
| 1 | 실현손익이 기록되지 않아 손실 한도·보호장치 6종이 동시 무력화 | **치명** ✅수정 | 정상 | 정상 |
| 2 | 한 회전 안에서 보유수·현금 한도가 종목 수만큼 곱해짐 | **치명** | 깨짐 | **더 심함** |
| 3 | [지금 1회전] 버튼이 확인 없이 실계좌에 실주문 | **치명** | — | **깨짐** |
| 4 | live 수동 청산이 조용히 무시될 수 있음 | **치명** | — | **깨짐** |
| 5 | 미결 주문이 안 풀리면 그 종목 손절이 영구 정지 | 높음 | 정상 | 깨짐 |
| 6 | 초단타 예산·일일손실이 유니버스 회전으로 새어나감 | 높음 | 깨짐 | 깨짐 |
| 7 | 복원된 미국 포지션이 통화 혼용으로 즉시 강제 청산 | 중간 | 깨짐 | 정상 |
| 8 | 엔진 루프 직렬 처리로 다중 계정 손절 지연 | 중간 | 깨짐 | 깨짐 |
| 9 | 쿨다운·거부대기가 프로세스 메모리에만 존재 | 중간 | 깨짐 | 깨짐 |
| 10 | 거부된 손절 주문이 60초간 재시도를 조용히 막음 | 중간 | 깨짐 | 깨짐 |

가장 무거운 것은 **1번**입니다. 문서 §3 안전장치 표에 적힌 "일일 손실 한도 -3%",
"최대 낙폭 -10%", 초단타의 "`daily_loss_krw` 오늘 이 금액을 잃으면 종료",
그리고 §8-c 보호장치 4종이 **실계좌에서 단 한 번도 발동할 수 없는 상태**입니다.
문서를 읽은 사람은 계좌가 지켜지고 있다고 믿게 됩니다.

재현: `python tests/test_autotrade_qa_repro.py`

---

## 2. 치명 결함

### 2.1 실계좌에서 실현손익이 한 번도 기록되지 않습니다

**뿌리** — `KISBroker` 는 주문 '접수'까지만 책임지고 `realized_pnl` 을 채우지 않습니다
(`engine/broker.py:712`, 기본값 `None`). 그런데 정산 패스는 그 값을 그대로 씁니다.

```python
# engine/autotrade.py:522
store.record_daily_trade(user_id, mode, record.get("realized_pnl") or 0)
#                                        ^^^^^^^^^^^^^^^^^^^^^ live 에서는 항상 None → 0
```

`_apply_fill` 은 체결가·체결수량·슬리피지는 갱신하지만 손익은 계산하지 않고,
다른 어떤 경로도 `at_orders.realized_pnl` 을 채우지 않습니다.
`PaperBroker` 만 `paper.sell()` 이 돌려준 실현손익을 넣습니다.

**파급** — 실계좌에서 다음이 전부 죽습니다.

| 안전장치 | 판단 재료 | live 에서의 값 |
|---|---|---|
| 일일 손실 한도 (-3%) | `at_daily.realized_pnl` | 0 |
| 최대 낙폭 한도 (-10%) | 〃 | 0 |
| 초단타 `daily_loss_krw` | `realized_today()` | 0원 / 0건 |
| 쿨다운·손절감시·낙폭감시·부진종목 | `closed_trades()` | 빈 목록 |
| 승률·실현손익 요약 | `summary()` | 0 |

특히 일일 손실 한도는 **손절할 때마다 오늘 손익이 0으로 리셋됩니다.**
오늘 손익 = `(현재 평가손익 − 시작 평가손익) + 실현손익` 인데, 손절하면
평가손실이 사라지고 실현손실은 0으로 들어오기 때문입니다.

재현 결과 (`live` 모드, 총자산 1,000만원):

```
장 시작   : 오늘손익 +0
손절 직전 : 오늘손익 -500,000 (-5.00%) → 차단 ['일일 손실 한도 초과 (-5.00% / 한도 -3%)']
손절 직후 : 오늘손익      +0 (+0.00%) → 차단 []          ← 손실이 증발
[대조군] paper 모드 손절 후 오늘손익 -500,000 — 정상 집계됨
```

**즉, 계좌는 연속 손절로 0원까지 녹을 수 있고 어떤 한도도 발동하지 않습니다.**

**수정 방향** — `_apply_fill` 이 청산 체결을 확정할 때 실현손익을 직접 계산해
`update_order(realized_pnl=...)` 로 채워야 합니다. KIS 체결내역은 손익을 주지
않으므로 엔진이 `at_position_state.entry_price` 와 `avg_fill_price`,
`inst.costs()` 로 계산하는 수밖에 없습니다. 이건 선택이 아니라 필수 경로입니다.
그때까지는 **`live` 를 켜면 안 됩니다.**

---

### 2.2 한 회전 안에서 리스크 한도가 종목 수만큼 곱해집니다

`_tick` 은 진입 루프 **시작 전에** `RiskEngine` 을 한 번 만들고
(`engine/autotrade.py:349`), 루프 안에서 갱신하지 않습니다.
`check_entry` 의 보유 종목 수·주문가능금액·총 노출 검사는 전부 그 스냅샷만 봅니다
(`engine/risk.py:150`, `161`, `184`, `198`).

`held` 집합은 갱신되지만 **즉시 체결된 경우에만**(`engine/autotrade.py:1355`)이고,
그나마 같은 종목 재진입만 막습니다. 실계좌 주문은 `pending` 이라 `held` 에도
`positions` 에도 들어가지 않습니다.

재현 결과 (max_positions=1, 총자산·주문가능금액 100만원):

```
승인된 주문: 5건 — AAA 99주(990,000원), BBB 99주(990,000원), CCC 99주(990,000원),
                  DDD 99주(990,000원), EEE 99주(990,000원)
주문 총액  : 4,950,000원  (계좌의 5.0배)
```

파생상품이 섞이면 더 나쁩니다. 코스피200 선물 1계약 명목이 8,750만원이므로
총 노출 한도가 회전당 종목 수만큼 곱해지면 계좌 대비 수십 배 노출이 나옵니다.

`paper` 는 원장이 현금을 확인해 뒤쪽 주문이 거부되지만, **보유 종목 수 상한은
paper 에서도 뚫립니다.** `live` 는 KIS 가 주문가능금액을 실시간으로 보므로
일부만 거부되고 — 즉 **몇 건이 나갈지가 비결정적**입니다.

**수정 방향** — 승인된 주문을 회전 안에서 되먹여야 합니다. `RiskEngine` 에
`reserve(inst, quantity, cost)` 를 두어 승인 즉시 가용현금·보유수·노출을 차감하고,
`_handle_entry` 가 주문 접수 후 호출하는 형태가 자연스럽습니다.

---

### 2.3 [지금 1회전] 이 확인 없이 실계좌에 실주문을 냅니다

`AUTOTRADE.md` §1-3 은 이렇게 안내합니다.

> 3. 우측 상단 **[지금 1회전]** 으로 판단만 먼저 확인합니다

실제로는 `force=True` 가 `enabled` 검사만 건너뛸 뿐, `dry_run` 은 설정값 그대로입니다.

```python
# api.py:1722
result = autotrade.run_once(user["id"], force=True)
```

`web/at_common.js:219` 의 핸들러에는 확인창이 없습니다. 따라서 mode=`live` 이고
`dry_run` 이 꺼져 있으면:

- 자동매매를 **[시작] 하지 않아도** 실주문이 나갑니다
- §5 가 약속한 **확인 문구 `LIVE` 입력 단계가 이 경로에서는 우회됩니다**
  (확인 문구는 `/api/autotrade/enable` 에만 걸려 있습니다)

문서가 "판단만"이라고 적어 둔 버튼이 실제로는 돈을 씁니다. 세 겹 안전장치
(환경변수 + 실주문 스위치 + 확인 문구) 중 마지막 겹이 여기서 빠집니다.

**수정 방향** — `run_now` 에서 `mode == live` 면 확인 문구를 요구하거나,
버튼이 `dry_run` 을 강제한 1회전을 돌리도록 바꾸고 실주문용은 따로 두는 것.

---

### 2.4 실계좌 수동 청산이 조용히 무시될 수 있습니다

```python
# api.py:2112
coid = broker.make_client_order_id(user["id"], inst.key, "manual_close")
#                                  ^ mode= 누락 → 기본값 PAPER
```

`make_client_order_id` 의 문서는 정확히 이 사고를 막으려고 mode 를 넣었다고
적혀 있습니다("모의계좌에서 낸 주문번호가 실전 주문을 막으면 안 됩니다").
그런데 수동 청산 엔드포인트만 인자를 빠뜨렸습니다.

```
엔진 자동청산 (live)  : 1:live:005930:close:202608081001
API 수동청산 (live)   : 1:paper:005930:manual_close:202608081001   ← paper 태그
API 수동청산 (paper)  : 1:paper:005930:manual_close:202608081001   ← 완전히 동일
```

같은 분에 paper 로 먼저 수동청산한 적이 있으면 `_already_sent`(프로세스 메모리)가
그 결과를 그대로 돌려주고 **live 주문은 전송되지 않습니다.** API 는 `ok: true` 와
paper 의 체결 내역을 응답하므로 화면에는 "청산됨"으로 보입니다.
사람이 급히 끊으려는 순간에 끊기지 않는 경로입니다.

---

## 3. 높음

### 3.1 미결 주문이 안 풀리면 그 종목의 손절이 영구히 멈춥니다

`_handle_exit` 과 `_handle_entry` 는 둘 다 미결 주문이 있으면 즉시 돌아갑니다
(`engine/autotrade.py:1028`, `1210`). 이중 주문 방지로는 옳지만, **미결 상태에서
빠져나오지 못하는 경로가 있습니다.**

- `_settle_open_orders` 는 `status.known == False` 일 때 **아무것도 하지 않습니다.**
  타임아웃 3배가 지나면 로그만 남기고, 포기하거나 사람에게 넘기는 종결 처리가 없습니다.
- `KISBroker.order_status` 는 국내 주문을 `stock_executions()` 로 조회하는데
  기본값이 `days_back=0`, 즉 **오늘 날짜만** 봅니다
  (`data_sources/kis_trading.py:804`). 날짜가 바뀌거나 서버가 꺼져 있던 사이에
  체결된 주문은 영원히 "주문 내역에서 찾지 못했습니다" 가 됩니다.
  (해외는 `days_back=3` 이라 이 문제가 없습니다 — 국내만 0입니다.)
- 취소 요청이 실패하면 `cancel_requested` 로 남는데 이 역시 `OPEN_STATUSES` 입니다.

결과적으로 그 종목은 `pending` 에 박힌 채, **손절·트레일링·시간 청산이 전부
검사되지 않는 포지션**이 됩니다. 전체 회전도 보호 회전도 같은 조기 return 을
쓰므로 두 경로 모두 막힙니다.

**수정 방향** — ① `stock_executions` 를 `days_back=3` 이상으로, ② 조회 불가가
일정 시간 이상 이어진 주문은 `unknown` 같은 종결 상태로 옮겨 미결 목록에서
빼되 사람에게 경고, ③ 최소한 청산 경로만은 미결 주문이 있어도
"수량 중복 없이" 진행할 수 있게 (보유수량 − 미결매도수량) 기준으로 판단.

### 3.2 초단타 예산·일일 손실 한도가 유니버스 회전으로 새어나갑니다

`scalp_budget_left` 는 "지금 초단타 유니버스에 있는 종목"의 평가금액만 차감합니다
(`engine/autotrade.py:1616`). `realized_today(symbols=scalp_universe(cfg))` 도
같은 기준입니다(`:1712`). 그런데 `refresh_scalp_universe` 는 **보유 중인 종목을
고정하지 않고** 15초마다 스크리너 결과로 갈아끼웁니다.

재현 결과:

```
AAA 500,000원 보유 · 예산 1,000,000원
AAA 가 초단타 유니버스에 있을 때 : 남은 예산 500,000원
AAA 가 유니버스에서 빠졌을 때    : 남은 예산 1,000,000원   ← 쓴 돈이 되돌아옴
```

문서 §8 은 "`budget_krw` — 초단타가 산 종목만 차감" 이라고 적었지만 구현은
"**지금 대상인 종목만** 차감"입니다. 대상은 15초마다 바뀌므로 실제 노출이
`budget_krw` 를 넘을 수 있고, 같은 이유로 그 종목에서 난 손실이
`daily_loss_krw` 집계에서 빠집니다.

**수정 방향** — 포지션의 주인은 `at_position_state.strategy` 에 정확히
기록돼 있습니다(`position_owner`). 유니버스가 아니라 그 태그로 집계하면 됩니다.
아울러 `refresh_scalp_universe` 가 보유 중인 종목을 `pinned` 처럼 유지해야 합니다.

---

## 4. 중간

### 4.1 복원된 미국 포지션이 통화 혼용으로 즉시 강제 청산 (paper)

`Position.avg_price` 의 통화가 브로커마다 다릅니다.

| 브로커 | `avg_price` | 근거 |
|---|---|---|
| `PaperBroker` | **원화** | `paper.buy(..., exec_krw)` 로 저장 (`engine/broker.py:354`) |
| `KISBroker` (해외) | **달러** | "단가는 달러 그대로 둡니다" (`engine/broker.py:636`) |

`_reconcile` 의 복원 경로(`engine/autotrade.py:950,962`)는 `position.avg_price` 에
`strategy.stop_distance`(호가 통화 = 달러)를 그대로 뺍니다.

```
paper 보유 avg_price = 420,000 (원화)   ← $300 × 1,400
stop_distance        = 6.00 (달러)
복원된 손절가         = 419,994.00  /  현재가 300.00 (달러)
→ check_exit 가 즉시 '손절 도달' 판정 → 복원 직후 전량 청산
```

### 4.2 엔진 루프가 사용자를 직렬 처리합니다

`EngineLoop._sweep` 은 `run_once` 를 동기로 호출합니다(`engine/autotrade.py:2003`).
전체 회전이 8종목에 약 24초 걸린다고 문서에 적혀 있으므로, 계정이 3개면
마지막 계정의 보호 회전이 최대 1분 이상 밀립니다. 손절 지연을 없애려고 만든
보호 회전의 목적이 다중 계정에서 무너집니다.

### 4.3 쿨다운·거부 대기가 프로세스 메모리에만 있습니다

`_last_scalp_exit`, `_broker_rejects`, `_scalp_phase`, `broker._sent_orders` 가
전부 모듈 전역 dict 입니다. 서버 재시작 시:

- 초단타 재진입 쿨다운이 풀립니다 → 손절 직후 재시작하면 같은 자리에 즉시 재진입
- 증권사 거부 30분 대기가 풀립니다 → 거부 사유(외화 부족 등)가 그대로인데 재시도
- 멱등 캐시가 비워집니다 (DB `order_exists` 가 받쳐주므로 이건 괜찮습니다)

문서는 "재시작 자체가 몇 초 이상 걸리므로 실질적 차이가 없습니다"라고 적었지만,
`protect_cooldown_loss_min` 기본값이 240분인 것과 비교하면 근거가 약합니다.

### 4.4 거부된 손절 주문이 60초간 재시도를 조용히 막습니다

```python
# engine/autotrade.py:1121
coid = make_client_order_id(user_id, inst.key, "close", mode=mode)
if store.order_exists(coid):
    return          # 로그 없음
```

`_record` 는 거부된 주문도 `status="rejected"` 로 원장에 넣으므로
`order_exists` 가 `True` 가 됩니다. 주문번호는 분 단위라 **손절 주문이 거부되면
그 분이 끝날 때까지(최대 60초) 다시 시도하지 않고, 그 사실이 로그에도 남지
않습니다.** 보호 회전을 10초로 돌려도 이 구간은 무방비입니다.

---

## 5. 문서 불일치

| 문서 | 실제 |
|---|---|
| §1-3 "[지금 1회전] 으로 판단만 먼저 확인합니다" | 실주문이 나갑니다 (2.3) |
| §3 "일일 손실 한도 -3% / 최대 낙폭 -10%" | 실계좌에서 발동 불가 (2.1) |
| §8 "`budget_krw` — 초단타가 산 종목만 차감" | 지금 대상인 종목만 차감 (3.2) |
| §8-c "보호장치 4종" | 실계좌에서 판단 재료가 비어 있음 (2.1) |
| §7 "미체결 자동취소 180초" | 조회 실패 시 종결 처리 없음 (3.1) |
| §5 "셋 중 하나라도 빠지면 주문은 거부됩니다" | 1회전 경로는 확인 문구를 안 봅니다 (2.3) |

---

## 6. 잘 되어 있는 것

균형을 위해 — 다음은 실제로 견고합니다.

- **청산 우선 순서**가 코드에도 그대로 반영돼 있습니다 (`_tick` 1→2단계)
- **fail-closed 원칙**이 일관됩니다. 계좌 평가금액 0, 시세 조회 실패, 브로커
  미준비에서 전부 멈춥니다
- **`known=False` 면 아무것도 하지 않는다**는 정산 원칙 자체는 옳습니다
  (다만 종결 경로가 없는 것이 3.1)
- **계좌(mode)별 완전 분리** — 포지션 상태·일자 기준선·주문 원장·주문번호가
  전부 mode 로 갈라져 있습니다
- **`manage_only_universe`** 로 사용자가 직접 산 주식을 건드리지 않습니다
- **부분 체결 시 실제 체결가로 손절선 재계산** (`_apply_fill`)
- **백테스트가 라이브와 같은 `evaluate()`/`check_exit()`** 를 쓰고,
  `allow_fetch=False` 로 미래 정보를 구조적으로 차단합니다
- **한도 클램프가 저장 시점에** 걸려 어느 경로로 읽어도 한도 안입니다

설계 문서의 판단은 대체로 옳고, 문제는 **그 판단이 `live` 경로에서 구현으로
이어지지 않은 지점**에 몰려 있습니다.

---

## 7. 권고

1. **지금 `live` 로 돌리고 있다면 중지하십시오.** 2.1 때문에 손실 한도가 없는
   상태로 도는 것과 같습니다.
2. 순서대로 고치는 것을 권합니다: **2.1 → 2.2 → 2.3 → 2.4 → 3.1 → 3.2**.
   앞의 넷은 돈이 직접 나가는 경로입니다.
3. 고친 뒤에는 **`mock`(모의투자 서버)에서** 회귀 확인을 해야 합니다.
   `paper` 는 이 결함 대부분을 재현하지 못합니다 — 그것이 이번 결함들이
   지금까지 남아 있던 이유입니다.
4. 회귀 테스트를 `tests/test_autotrade.py` 에 붙이십시오. 특히
   "live 모드에서 손절 후 `day['pnl']` 이 음수인가" 한 줄이 2.1 을 영구히 막습니다.
