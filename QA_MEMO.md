# QA 메모장 — 자동매매

> 작업용 메모입니다. 분석 근거와 재현 로그는 [QA_AUTOTRADE.md](QA_AUTOTRADE.md) 에 있고,
> **여기는 "무엇을 어떻게 고쳐 달라"만** 적습니다. 고칠 때마다 체크박스를 채우고
> 맨 아래 기록란에 날짜를 남겨 주세요.
>
> **작성** 2026-08-08 · **재현** `python tests/test_autotrade_qa_repro.py` (결함 남아 있으면 종료코드 1)

```
□ 미착수    ▣ 진행중    ✅ 완료·검증됨    ⏸ 보류(사유 적기)
```

---

## 🚨 코드를 고치기 전에 지금 당장

> C-1 은 고쳤지만 **C-2·C-3·C-4 가 남아 있어 아래 권고는 그대로 유효합니다.**

- [ ] **`live` 모드 자동매매를 중지** — 한 회전에 한도의 몇 배가 나갈 수 있습니다 (C-2)
- [ ] `live` 로 돌린 이력이 있다면 **증권사 앱에서 실제 잔고·미체결을 직접 대조** — 콘솔 숫자를 믿지 마세요 (C-2)
- [ ] C-1 이전에 `live`·`mock` 으로 낸 청산 기록은 **손익이 비어 있습니다.**
      과거 승률·실현손익 통계는 그 구간을 빼고 읽으세요 (소급 계산하지 않았습니다)
- [ ] 임시 방편이 필요하면 `dry_run: true` 로 두고 로그만 관찰
- [ ] `kill_switch` 는 **신규 진입만** 막습니다. 완전히 세우려면 `enabled: false`

---

## C. 치명 — 돈이 직접 나가는 경로

### ✅ C-1. 실계좌에서 실현손익이 기록되지 않아 안전장치 6종이 죽어 있음

> **2026-08-08 수정 완료.** 아래 요청대로 반영했고 회귀 검사 16건이 통과합니다.
> 바뀐 파일 — [engine/autotrade.py](engine/autotrade.py) (`_exit_realized_krw` 신규 ·
> `_apply_fill` · `_handle_exit`), [storage/autotrade.py](storage/autotrade.py)
> (`record_daily_trade` 에 `count` 추가).
> 기존 스위트 회귀 없음 (`test_autotrade.py` 305/305, `test_algo.py` 96/96).

| | |
|---|---|
| **증상** | `live`·`mock` 에서 손절할 때마다 오늘 손익이 0으로 리셋. 일일 손실 한도·최대 낙폭·초단타 `daily_loss_krw`·보호장치 4종이 **영원히 발동하지 않음** |
| **위치** | [engine/broker.py:712](engine/broker.py:712) · [engine/autotrade.py:496](engine/autotrade.py:496) `_apply_fill` · [engine/autotrade.py:522](engine/autotrade.py:522) |
| **원인** | `KISBroker` 는 접수까지만 책임지고 `realized_pnl` 을 `None` 으로 둠. 정산 패스가 `record.get("realized_pnl") or 0` 으로 받아 항상 0 을 기록 |
| **paper** | 정상 (`paper.sell()` 이 손익을 돌려줌) — 그래서 모의계좌로는 절대 안 드러납니다 |

**요청**

1. `_apply_fill` 이 **청산 체결을 확정하는 순간 실현손익을 직접 계산**해서
   `store.update_order(record["id"], realized_pnl=...)` 로 채워 주세요.
   KIS 체결내역은 손익을 주지 않으므로 엔진이 계산하는 수밖에 없습니다.
2. 계산 재료는 이미 다 있습니다 — `at_position_state.entry_price`(진입가),
   `status.avg_fill_price`(실체결가), `status.filled_quantity`, `inst.multiplier`,
   `inst.costs()`(수수료·거래세). 미국 종목은 **환산 시점을 명시**해 주세요.
3. 부분 체결이면 **그 회차에 체결된 수량분만** 계상해야 합니다
   (`newly = status.filled_quantity - already` 가 이미 계산돼 있습니다).
4. 진입가를 모르는 포지션(재동기화 복원분)은 `realized_pnl` 을 `None` 으로 남기되
   **로그로 알려** 주세요. 0 으로 채우면 승률·손익 통계가 조용히 오염됩니다.

**완료 기준**

- [x] `live` 모드에서 손절 후 `touch_daily()["pnl"]` 이 음수
- [x] `realized_today()["closed_count"]` 가 증가
- [x] `closed_trades()` 가 그 매매를 돌려줌 → 보호장치 4종이 판단 재료를 받음
- [x] 회귀 스위트 C-1 섹션 16건 전부 PASS

**무엇을 어떻게 계산하는가** (나중에 읽을 사람을 위해)

```
realized = (청산가 − 진입가) × 수량 × 승수 × 방향
           − 진입 수수료 − 진입 거래세 − 청산 수수료 − 청산 거래세
           → 호가 통화 기준이므로 fx.to_krw 로 원화 환산
```

- 규약은 `storage/paper.py` 의 매도와 동일 — **왕복 비용을 전부 뺀 순손익**
- **부분 체결**: 매번 누적 실현손익을 다시 계산하고 **늘어난 만큼만** 일자 집계에
  더합니다. 매매 건수는 `count=0` 으로 넘겨 전량 체결 때 한 번만 셉니다
- **미국 종목**: 달러 손익을 환산합니다. 안 하면 손실을 1/1,400 로 보게 됩니다
- **진입가를 모르면** `None` 으로 두고 경고 로그를 남깁니다. 0 으로 채우면
  손실이 없었던 것처럼 보입니다
- 즉시 체결 경로(`_handle_exit`)도 같은 정책으로 맞췄습니다 — 손익이 비면
  직접 계산하고, 끝까지 못 구하면 **아예 적지 않습니다**

**남은 확인 (실계좌에서만 가능)**

- [ ] `mock` 서버에서 실제 왕복 1건 — 계산된 실현손익이 증권사 화면과 맞는지
- [ ] 미국 종목 왕복 1건 — 환율 적용 시점 차이가 얼마나 나는지
      (포지션 평가는 KIS 기준환율, 여기는 `fx.usd_krw()` 를 씁니다)

---

### [ ] C-2. 한 회전 안에서 리스크 한도가 종목 수만큼 곱해짐

| | |
|---|---|
| **증상** | max_positions=1 · 주문가능금액 100만원 설정에서 **5종목 495만원(계좌의 5배)** 승인 |
| **위치** | [engine/autotrade.py:349](engine/autotrade.py:349) · [engine/risk.py:150](engine/risk.py:150) `보유 종목 수` / `:184` `총 노출` / `:198` `가용 현금` |
| **원인** | `RiskEngine` 을 진입 루프 **시작 전에 한 번만** 만들고 갱신하지 않음. 모든 종목이 같은 "보유 0종목·현금 전액"을 봄 |
| **왜 live 가 더 나쁜가** | 접수 주문은 `pending` 이라 `positions` 에도 `held` 에도 안 들어감. paper 는 원장이 현금을 막아주지만 **보유 종목 수 상한은 paper 에서도 뚫림** |

**요청**

1. `RiskEngine` 에 `reserve(inst, quantity, unit_cost, notional)` 를 추가해
   **승인 즉시 가용현금·보유종목수·총노출을 차감**하게 해 주세요.
2. `_handle_entry` 가 주문 접수(`order.ok`) 직후 `engine_risk.reserve(...)` 를 호출.
   접수만 된 주문도 예약으로 잡아야 합니다 — 체결될 수도 있는 돈입니다.
3. 스냅샷 고정 자체는 옳은 설계입니다(회전 중 비결정성 방지). **예약분만 별도로
   누적**하고 스냅샷은 그대로 두는 방식을 권합니다.
4. 파생이 섞이면 명목금액 기준이라 더 위험합니다 — 총 노출 예약을 빠뜨리지 마세요.

**완료 기준**

- [ ] 같은 회전에서 `max_positions` 를 넘는 진입이 승인되지 않음
- [ ] 승인된 주문들의 합계가 `available_cash` 를 넘지 않음
- [ ] 거부 사유에 "이번 회전에서 이미 예약됨"이 사람이 읽을 수 있게 남음
- [ ] 재현 테스트의 결함 2) 두 항목이 PASS

---

### [ ] C-3. [지금 1회전] 버튼이 확인 없이 실계좌에 실주문

| | |
|---|---|
| **증상** | 자동매매를 [시작]하지 않아도, `LIVE` 확인 문구를 입력하지 않아도 실주문이 나감 |
| **위치** | [api.py:1722](api.py:1722) `run_once(force=True)` · [web/at_common.js:219](web/at_common.js:219) `runOnce()` |
| **원인** | `force=True` 는 `enabled` 검사만 건너뜀. `dry_run` 은 설정값 그대로이고 기본값이 `False` |
| **문서 불일치** | AUTOTRADE.md §1-3 "**[지금 1회전] 으로 판단만 먼저 확인합니다**" — 사실이 아닙니다 |
| **안전장치 구멍** | §5 의 3단계(환경변수 + 실주문 스위치 + 확인 문구) 중 **확인 문구가 이 경로에서 빠짐** |

**요청** — 셋 중 하나를 골라 주세요. 1번을 권합니다.

1. **버튼은 항상 `dry_run` 강제**. 실주문 1회전이 필요하면 별도 버튼 + 확인창.
   문서가 이미 "판단만"이라고 약속했으니 문서에 코드를 맞추는 쪽입니다.
2. `mode == live` 면 API 가 `confirm == "LIVE"` 를 요구 (enable 과 동일 규칙).
3. 프런트에서 live 일 때만 확인 모달. — **단독으로는 부족합니다.** API 를 직접
   때리면 그대로 뚫립니다.

**완료 기준**

- [ ] live 모드에서 [지금 1회전]이 확인 없이 주문을 내지 않음
- [ ] API 직접 호출로도 우회되지 않음
- [ ] AUTOTRADE.md §1-3 문구가 실제 동작과 일치

---

### [ ] C-4. 실계좌 수동 청산이 조용히 무시될 수 있음

| | |
|---|---|
| **증상** | live 수동 청산 주문이 전송되지 않았는데 API 는 `ok: true` + 체결 화면 |
| **위치** | [api.py:2112](api.py:2112) |
| **원인** | `make_client_order_id(user_id, key, "manual_close")` — **`mode=` 인자 누락** → 항상 `paper` 태그 |

```
엔진 자동청산 (live)  : 1:live:005930:close:202608081001
API 수동청산 (live)   : 1:paper:005930:manual_close:202608081001   ← paper 로 찍힘
API 수동청산 (paper)  : 1:paper:005930:manual_close:202608081001   ← 완전히 동일
```

같은 분에 paper 로 청산한 적이 있으면 `broker._already_sent`(프로세스 메모리)가
그 결과를 돌려주고 live 주문은 나가지 않습니다.
`make_client_order_id` 의 docstring 이 정확히 이 사고를 막으려고 mode 를 넣었다고
적혀 있는데, 이 엔드포인트만 빠뜨렸습니다.

**호출 지점 전수 확인 결과** — 프로덕션 코드에서 **딱 한 곳만** 빠져 있습니다.

| 위치 | mode | |
|---|---|---|
| `engine/autotrade.py:1121` 자동 청산 | `mode=mode` | ✅ |
| `engine/autotrade.py:1320` 자동 진입 | `mode=mode` | ✅ |
| **`api.py:2113` 수동 청산** | **없음 → `paper`** | ❌ |

> 참고로 `tests/test_autotrade.py:1216` 에 "계좌가 다르면 다른 주문번호"를 확인하는
> 테스트가 **이미 있습니다.** 그런데 헬퍼 함수를 직접 호출해 검사하기 때문에,
> 헬퍼를 잘못 쓰는 호출부는 잡지 못했습니다. P-6 의 대표 사례입니다.

**요청**

1. `mode=cfg.get("mode", "paper")` 를 넘겨 주세요. **한 줄입니다.**
2. `mode` 를 기본값 있는 선택 인자가 아니라 **필수 인자**로 바꾸는 것을 권합니다.
   기본값 `PAPER` 가 있어서 빠뜨려도 조용히 동작한 것이 원인입니다.
   필수로 바꾸면 같은 실수가 import 시점에 터집니다.
3. `record_order` 가 `IntegrityError` 로 `None` 을 돌려줄 때(=중복 주문번호)
   API 가 성공으로 응답하지 않게 해 주세요. 지금은 기록 실패를 무시하고
   `ok: true` 를 돌려줍니다.

**완료 기준**

- [ ] live 수동 청산의 주문번호에 `live` 가 들어감
- [ ] 중복 주문번호로 기록에 실패하면 API 가 에러를 반환
- [ ] `make_client_order_id` 호출 지점 3곳 전부에 `mode` 존재

---

## H. 높음

### [ ] H-1. 미결 주문이 안 풀리면 그 종목의 손절이 영구히 멈춤

| | |
|---|---|
| **위치** | [engine/autotrade.py:1028](engine/autotrade.py:1028) · `:1210` · `_settle_open_orders` `:448` · [data_sources/kis_trading.py:804](data_sources/kis_trading.py:804) |
| **원인 3가지** | ① `status.known == False` 일 때 **종결 처리가 없음**(로그만) ② 국내 체결조회가 `days_back=0` = **오늘만** 조회 ③ 취소 실패 시 `cancel_requested` 로 남는데 이것도 미결 상태 |

날짜가 바뀌거나 서버가 꺼져 있던 사이 체결된 국내 주문은 영원히
"주문 내역에서 찾지 못했습니다"가 되고, `has_open_order` 가 계속 `True` 라
**전체 회전도 보호 회전도 그 종목의 손절을 검사하지 않습니다.**
(해외는 `days_back=3` 이라 문제 없음 — 국내만 0입니다.)

**요청**

1. `stock_executions` 기본값을 `days_back=3` 이상으로.
2. 조회 불가가 일정 시간(예: `order_timeout_sec × 10`) 이상 이어지면
   `unknown` 같은 **종결 상태**로 옮겨 미결 목록에서 빼고 사람에게 경고.
   "모르면 건드리지 않는다"는 옳지만, **영원히 모르는 상태로 두는 것**은 다릅니다.
3. 최소한 **청산 경로만은** 미결 주문이 있어도 진행할 수 있게 —
   `(보유수량 − 미결 매도수량)` 을 기준으로 판단하면 이중 청산 없이 가능합니다.
4. 콘솔에 "N분째 상태 미확인 주문" 배지를 띄워 주세요. 지금은 로그에만 묻힙니다.

**완료 기준**

- [ ] 서버를 껐다 켠 뒤에도 어제 주문이 정상 종결됨
- [ ] 미결 주문이 있는 포지션도 손절 검사가 돎(또는 못 도는 사실이 화면에 뜸)

---

### [ ] H-2. 초단타 예산·일일 손실이 유니버스 회전으로 새어나감

| | |
|---|---|
| **증상** | 50만원 쓴 종목이 유니버스에서 빠지자 남은 예산이 100만원으로 복귀 |
| **위치** | [engine/autotrade.py:1616](engine/autotrade.py:1616) `scalp_budget_left` · `:1712` `realized_today(symbols=...)` · `:765` `refresh_scalp_universe` |
| **원인** | 둘 다 "**지금** 초단타 유니버스에 있는 종목"으로 집계. 유니버스는 15초마다 갈림 |
| **문서 불일치** | §8 "`budget_krw` — **초단타가 산 종목만** 차감" ↔ 구현은 "지금 대상인 종목만" |

**요청**

1. 집계 기준을 유니버스가 아니라 **`at_position_state.strategy` 태그**로 바꿔
   주세요. `position_owner()` 가 이미 정확히 그 값을 돌려줍니다 —
   "포지션은 진입할 때 주인이 박힙니다"라는 설계가 이미 있는데 예산 계산만
   그걸 안 쓰고 있습니다.
2. `refresh_scalp_universe` 가 **보유 중인 초단타 종목을 `pinned` 처럼 유지**하게
   해 주세요. 안 그러면 들고 있는 종목이 추적 목록에서 사라져 틱 시세도 끊깁니다.
3. `realized_today` 도 같은 기준으로 — 그 종목 손실이 `daily_loss_krw` 에서
   빠지면 한도가 헐거워집니다.

**완료 기준**

- [ ] 보유 종목이 유니버스에서 빠져도 남은 예산이 그대로
- [ ] 보유 중인 초단타 종목은 유니버스 갱신에서 빠지지 않음
- [ ] 재현 테스트의 결함 3) PASS

---

## M. 중간

### [ ] M-1. 복원된 미국 포지션이 통화 혼용으로 즉시 강제 청산 (paper)

`Position.avg_price` 의 통화가 브로커마다 다릅니다 —
`PaperBroker` 는 **원화**([engine/broker.py:354](engine/broker.py:354)),
`KISBroker` 해외는 **달러**([engine/broker.py:636](engine/broker.py:636)).
`_reconcile` ([engine/autotrade.py:950](engine/autotrade.py:950)) 이 원화 평단에서
달러 ATR 을 빼서 손절가 419,994 vs 현재가 300 같은 값이 나옵니다 → 복원 즉시 전량 청산.

**요청** — `Position` 에 `currency` 필드를 추가해 **통화를 값과 함께** 들고 다니게
해 주세요. 주석으로 "여기는 원화, 저기는 달러"를 관리하는 방식은 이미 한 번
틀렸습니다. 최소 조치로는 `_reconcile` 에서 `inst.currency` 를 보고 환산.

### [ ] M-2. 엔진 루프가 사용자를 직렬 처리

[engine/autotrade.py:2003](engine/autotrade.py:2003) 이 `run_once` 를 동기 호출.
전체 회전이 8종목에 약 24초이므로 계정 3개면 마지막 계정의 보호 회전이 1분 이상 밀립니다.
손절 지연을 없애려고 만든 보호 회전의 목적이 다중 계정에서 무너집니다.
**요청** — 사용자별 워커 스레드 또는 보호 회전만이라도 전체 회전과 분리.
(단일 계정이면 무해하므로 우선순위는 낮습니다.)

### [ ] M-3. 거부된 손절 주문이 60초간 재시도를 조용히 막음

[engine/autotrade.py:1121](engine/autotrade.py:1121) — `_record` 가 거부된 주문도
원장에 넣어 `order_exists(coid)` 가 `True` 가 됩니다. 주문번호는 분 단위라
**손절이 거부되면 그 분이 끝날 때까지 재시도하지 않고 로그도 안 남습니다.**
보호 회전을 10초로 돌려도 이 구간은 무방비입니다.
**요청** — 거부된 주문은 중복 판정에서 제외하거나, 청산용 주문번호에 초/시퀀스를
넣어 주세요. 최소한 **조용한 return 을 없애고 로그**를 남겨야 합니다.

### [ ] M-4. 쿨다운·거부 대기가 프로세스 메모리에만 존재

`_last_scalp_exit` · `_broker_rejects` · `_scalp_phase` 가 전부 모듈 전역 dict 입니다.
서버 재시작 시 초단타 재진입 쿨다운과 증권사 거부 30분 대기가 풀립니다.
`protect_cooldown_loss_min` 기본값이 240분인 것과 비교하면
"재시작은 몇 초 걸리니 실질 차이 없다"는 주석의 근거가 약합니다.
**요청** — 최소한 재진입 쿨다운은 DB(`at_events` 또는 전용 테이블)로 옮겨 주세요.

---

## P. 더 다듬을 것 (급하지 않지만 쌓이면 문제)

- [ ] **P-1. 매매 경로에 연결되지 않은 신규 모듈** — `engine/microstructure.py`,
      `engine/pairs.py` 를 참조하는 곳이 없습니다(테스트 제외). `econophysics.py` 는
      `indicators.py`·`recommender.py` 에 연결돼 있어 대조가 됩니다.
      **연결할 것인지, 실험용으로 남길 것인지 파일 머리에 한 줄로 밝혀** 주세요.
      지금은 "돌고 있는 줄 알았는데 아니었다"가 가능한 상태입니다.
- [ ] **P-2. 프로세스 전역 캐시가 무한히 증가** — `_last_signal_error`,
      `_skipped_notice`, `_broker_rejects`, `_scalp_phase`, `_us_exchange_cache` 에
      정리 로직이 없습니다. `broker._sent_orders` 만 상한(2000)이 있습니다.
      장기 가동 서버에서 종목을 계속 갈아끼우는 초단타 특성상 실제로 쌓입니다.
      TTL 또는 상한을 넣어 주세요.
- [ ] **P-3. `make_client_order_id` 의 `mode` 를 필수 인자로** — C-4 의 재발 방지.
      기본값이 있어서 빠뜨려도 조용히 동작한 것이 원인입니다.
- [ ] **P-4. AUTOTRADE.md 섹션 번호가 뒤엉킴** — `§7 → §8 → §7-b → §8-b …` 순서로
      나옵니다(7-b 가 8 뒤). 목차를 따라 읽으면 길을 잃습니다.
- [ ] **P-5. 문서를 실제 동작에 맞추기** — QA_AUTOTRADE.md §5 의 대조표 6건.
      특히 §3 안전장치 표는 **실계좌에서 발동하지 않는 항목**을 그대로 싣고 있어,
      읽는 사람이 계좌가 지켜지고 있다고 믿게 됩니다. C-1 을 고치기 전이라면
      표에 경고를 다는 것만이라도.
- [ ] **P-6. 테스트가 헬퍼는 검사하는데 호출부는 검사하지 않음** —
      `tests/test_autotrade.py` 는 1,500줄이고 계산 로직은 촘촘합니다. 그런데
      **`live` 모드 정산 경로를 통과하는 테스트가 하나도 없습니다.**
      C-4 가 대표적입니다. "계좌가 다르면 주문번호가 다르다"는 테스트가 이미
      있는데도 그 헬퍼를 잘못 쓰는 엔드포인트를 못 잡았습니다.
      **단위가 아니라 경로를 태우는 테스트**가 필요합니다 — 이번 결함들이
      지금까지 남아 있던 구조적 이유입니다. 아래 R 체크리스트 참고.

---

## R. 회귀 테스트 체크리스트 (고친 뒤 반드시)

`tests/test_autotrade.py` 에 붙여 주세요. 한 줄짜리가 결함 하나를 영구히 막습니다.

`tests/test_autotrade_qa_repro.py` 가 이 목록을 담고 있습니다 —
**메모의 결함 번호와 섹션이 1:1 로 대응**하고, 남은 결함은 FAIL 로 뜹니다.

- [x] `live` 모드에서 손절 체결 후 `touch_daily()["pnl"] < 0` — **C-1 을 영구 차단**
- [x] `live` 청산 1건 후 `closed_trades()` 가 1건 반환 — 보호장치 재료
- [x] 부분 체결이 손익을 이중 계상하지 않고 매매 건수를 부풀리지 않음
- [x] 달러 손익이 원화로 환산됨 / 선물 숏의 부호·승수
- [x] 진입가를 모르면 0 이 아니라 `None` + 경고
- [ ] `max_positions=1` 로 한 회전에 3종목 신호 → 진입 1건만
- [ ] 승인된 주문 합계 ≤ `available_cash`
- [ ] 보유 종목이 초단타 유니버스에서 빠져도 `scalp_budget_left` 불변
- [ ] `make_client_order_id` 호출 지점 전부에 `mode` 존재 (정적 검사로도 가능)
- [ ] 어제 날짜 미결 주문이 다음 날 정상 종결
- [ ] 원화 평단 포지션 복원 시 손절가가 현재가보다 아래

**검증 환경** — `paper` 로 통과했다고 끝내지 마세요. 이번 치명 4건 중 3건이
`paper` 에서는 정상 동작합니다. **`mock`(KIS 모의투자 서버)에서 한 번 더** 돌려야
실주문 경로가 검증됩니다.

---

## N. 이번 라운드에서 확인하지 못한 것 (다음 QA)

장이 닫혀 있고 실계좌 키로 주문을 낼 수 없어 **코드 심사로만** 본 항목입니다.
"문제 없음"이 아니라 "**확인 안 됨**"입니다.

- [ ] 부분 체결의 실제 동작 (KIS 응답 필드 배치, `remain_qty` 해석)
- [ ] KIS 실시간 WebSocket 구독 한도 학습 (문서값 41 vs 실제)
- [ ] 미국 지연 시세 차단 (`max_quote_delay_sec` 5초 하드 한도)이 실제로 걸리는지
- [ ] 파생 만기 롤오버 · `FUT_FRONT` 별칭이 만기 당일 무엇을 가리키는지
- [ ] 통합증거금 계좌에서 `account()` 의 `total_value` 재구성이 증권사 화면과 일치하는지
- [ ] 장중 4초 폴링 · 초봉 누적 · 실시간 차트 마커
- [ ] 공휴일 판정 (내장 시간표만 쓰면 공휴일에 "장중"으로 뜬다고 QA_REPORT.md §6.2 에 기록됨)

---

## 기록

| 날짜 | 항목 | 내용 | 확인 |
|---|---|---|---|
| 2026-08-08 | — | QA 1차. 결함 10건(치명 4) 발견, `live` 가동 중지 권고 | 회귀 스위트 6건 FAIL |
| 2026-08-08 | **C-1** | 청산 실현손익을 엔진이 직접 계산해 원장·일자집계에 기록. 부분 체결·통화 환산·숏 승수·진입가 미상까지 처리 | C-1 16건 PASS · 기존 305/305, 96/96 회귀 없음 |
| | | | |
