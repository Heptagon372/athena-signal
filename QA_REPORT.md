# 아테나 시그널 QA 심사보고서

**버전** v0.8 · **일시** 2026-08-03 KST · **판정** 조건부 합격
**테스트** `python tests/test_qa.py` → **173/173 통과 (100%)**

---

## 1. 요약

| 분류 | 결과 | 비고 |
|---|---|---|
| 계량 추정량 | 10/10 | 몬테카를로로 참값 복원 확인 |
| 경계 조건 | 8/8 | 빈 입력·무변동·표본부족 방어 |
| 시평선 모델 | 13/13 | 6개 시평선 단조성 + 개장예측 공식 항등성 |
| 감성 분석 | 14/14 | 어형변화·비포화·부정어 반전 |
| 스크랩 저장소 | 6/6 | 중복 제거·만료·출처 화이트리스트 |
| 예측 기록·채점 | 8/8 | **중복 저장 방지·만기 판정·시평선별 채점·무효 처리** |
| 모의투자 | 17/17 | **수수료·거래세·환율 환산·손익 정합성** |
| 공식 API 계층 | 17/17 | 미설정 폴백·캘린더 파싱·**공휴일 판정** |
| 장 세션 | 13/13 | 한국 5구간 · 미국 3구간 |
| 종목 해석 | 14/14 | 실존 검증 + 오타 차단 |
| 데이터 소스 | 17/17 | 전 소스 실제 도달 + **해외 커뮤니티 관련성 필터** |
| API 엔드포인트 | 36/36 | 응답·차단·성능·공식 일관성·스크랩 왕복 |

### 스크랩 파이프라인 검증

- 빈 제목·2자 미만은 거부, 동일 제목은 지문으로 중복 판정 ✅
- 화이트리스트 밖 출처(`evil-source`)는 `other` 로 정규화 ✅
- 만료 기간 밖 게시글은 조회에서 제외 ✅
- POST → 조회 → 커뮤니티 여론 병합까지 왕복 확인 ✅
- 없는 종목으로 스크랩 전송 시 404 차단 ✅

### 장 시작 전 예측 검증

화면에 표시되는 공식이 실제 계산과 일치하는지 항등성을 검사합니다.

- 각 항이 `가중치 × 점수 × 배수` 로 정확히 재현되는가 ✅
- 세 항의 합이 `raw_score` 와 일치하는가 (오차 < 0.001) ✅
- 중립 입력(0,0,0.5) → 정확히 50.0% ✅
- `/predict` 와 `/quote` 의 개장예측이 동일한가 ✅

재현:

```bash
python tests/test_qa.py
```

네트워크 없이 계산 로직만 검증하려면 `--offline` 을 붙입니다.

---

## 2. 계량 추정량 검증 (핵심)

성질이 **알려진** 인공 시계열을 생성해 추정량이 그 성질을 복원하는지 확인했습니다.
실제 주가로는 참값을 모르므로 검증이 불가능하기 때문입니다.

### 2.1 Hurst 지수 — 편향 보정 전후

R/S 분석은 소표본에서 H를 과대추정하는 것이 알려져 있습니다. Anis-Lloyd(1976)
보정을 적용해 몬테카를로 25회 평균으로 확인했습니다.

| 시계열 | 참값 | 보정 전 | **보정 후** |
|---|---|---|---|
| 랜덤워크 | 0.500 | 0.639 | **0.513** |
| 평균회귀(OU) | < 0.5 | 0.453 | **0.327** |
| 모멘텀 AR(1) φ=0.35 | > 0.5 | 0.744 | **0.618** |

보정 전에는 **랜덤워크조차 "추세 지속"으로 오판**했습니다. 실제 삼성전자도
0.585(추세지속) → 0.459(랜덤)으로 교정되었습니다. 이 오판은 추세추종 지표에
잘못된 가중치를 실어 확률을 왜곡시킵니다.

### 2.2 분산비율 검정 (Lo–MacKinlay 1988)

| 시계열 | VR(5) | z통계량 | 랜덤워크 기각 |
|---|---|---|---|
| 평균회귀 | 0.681 | −3.30 | ✅ 기각 (올바름) |
| 랜덤워크 | 1.100 | +0.98 | ❌ 기각 못함 (올바름) |

> **주의**: 드리프트만 있는 추세 시계열은 VR ≈ 1이 나옵니다. 분산비율은
> *자기상관*을 검정하는 것이지 *추세*를 검정하는 것이 아니기 때문이며,
> 이는 오류가 아니라 정의대로 동작하는 것입니다.

### 2.3 기타

- **Yang-Zhang 변동성**: 일간 σ=2%로 생성한 계열에서 연율 31.7% 기대 → 복원 확인
- **OU 반감기**: 평균회귀 계열에서 2.54봉 검출, 추세 계열에서는 `None` 반환
- **추세 회귀**: 완전 지수추세에서 R² > 0.999

---

## 3. 발견하고 수정한 결함

| # | 결함 | 영향 | 조치 |
|---|---|---|---|
| 1 | Hurst 소표본 상향편향 | 랜덤워크를 추세로 오판 → 가중치 왜곡 | Anis-Lloyd 보정 적용 |
| 2 | 년봉 200개 요청 시 HTTP 500 | 300년 역산이 pandas Timedelta 한계(292년) 초과 | 조회 상한 68년으로 클램프 |
| 3 | 감성 점수 포화 | `(pos−neg)/(pos+neg)`가 한쪽만 걸리면 무조건 ±1.0 | `tanh(가중합/2)`로 교체 |
| 4 | 영어 어형변화 미매칭 | `Surging`·`Tumbling` 점수 0 | 어간+어미 정규식 (`top`이 `topic`에 오매칭되지 않도록 경계 강제) |
| 5 | 인버스 ETF 부호 오류 | SOXS에 반도체 호재를 그대로 호재로 계산 | 종목 미언급 섹터 뉴스만 부호 반전 |
| 6 | 6개 시평선이 동일 확률 | 시점 구분이 무의미 | 시평선별 분봉/일봉 배합 + 신뢰도 감쇠 |
| 7 | 한글 조사 처리 | "지표은(는)" 출력 | 받침 판별로 은/는 자동 선택 |
| 8 | 성능 측정 오류 (테스트 자체 결함) | urllib 연결 비용을 서버 처리시간으로 오인 | 세션 재사용 측정 + 기준선 차감 |
| 9 | **RSS 무한 대기** | `feedparser.parse(url)` 은 자체 타임아웃이 없음. 뉴스 피드를 6개로 늘리자 `/api/predict` 전체가 멈춤 | 타임아웃 있는 `http_client` 로 먼저 받고 문자열만 파싱 (8초) |
| 10 | 외부 피드 429로 QA 오탐 | Yahoo RSS 순간 제한을 코드 결함으로 오판 | 재시도 후 판정하도록 테스트 보완 |

| 11 | **폴링이 예측 기록을 오염** | 대시보드가 4초마다 `/predict` 를 호출할 때마다 예측 6건이 새로 저장됨. 1분에 136건, 삼성전자 1일 예측만 129건 중복 | 같은 (종목, 시평선)의 미채점 예측이 15분 안에 있으면 INSERT 대신 UPDATE |
| 12 | **채점이 시평선을 무시** | 10분 예측을 하루 등락으로 채점. 시평선 6종 도입 시 채점 로직을 함께 고치지 않음 | 예측별 `target_at`·`base_price` 저장 후, 하루 미만은 분봉·이상은 일봉으로 각자 기간을 채점 |
| 13 | **만기 전 채점** | 방금 낸 '1일 뒤' 예측(1,439분 남음)까지 즉시 채점 | `get_matured_predictions()` 로 만기 도래분만 선별 |
| 14 | **무변동을 상승으로 판정** | `final >= base` 라 가격 변동이 없으면 전부 '상승 적중' → 적중률이 상승 쪽으로 부풀려짐 | `final == base` 는 채점에서 제외하고 건수를 보고 |
| 15 | 구/신 시평선 혼재 집계 | `open/5min/30min/close`(1,472건)와 신규 시평선(1,278건)을 무구분 합산 | 통계를 시평선별로 분해하고 구버전 라벨을 표시 |

| 16 | **원화 계좌에 달러 가격 미환산** | 모의투자에서 100만원으로 AAPL 3,229주(약 14억원어치) 매수 — 통화 환산 없이 KRW 금액을 USD 가격으로 나눔 | `data_sources/fx.py` 추가, 미국 종목 가격을 원화로 환산 후 거래·평가 |
| 17 | 채점 불가 예측이 대기열에 영구 적체 | 장 마감 후 낸 10분 예측은 만기 구간에 거래가 없어 영원히 채점 불가 | 6시간 유예 후 '무효' 처리 (적중률 통계에는 미포함) |

**결함 16 상세** — 모의투자 첫 실행에서 발견했습니다. 계좌는 원화 단일 통화인데
미국 종목은 달러로 호가되므로, 환산 없이 계산하면 자산이 약 1,400배로 부풀려집니다.
환율은 토스 공식 API → Yahoo `KRW=X` 순으로 조회하고, 실패 시 보수적 기본값을
'추정치'로 표시합니다.

**결함 11~14 상세** — 이번 점검의 핵심입니다. QA 143건이 전부 통과하는 상태에서도
**적중률 통계 전체가 무의미**했습니다. 정리 직전 DB는 2,750건 중 400건이 채점돼
있었고 **적중률이 시평선 4종 모두 정확히 95.0%** 였는데, 이는 예측력이 아니라
수백 건의 중복 예측을 같은 하루 등락으로 일괄 채점한 산물이었습니다.

수정 후 폴링 10회에 저장은 6건(시평선당 1건)으로 유지되고, 채점은 만기가 지난
예측만 각자의 기간으로 이뤄집니다. 오염된 기록 2,750건은 전량 삭제해 트랙레코드를
0에서 다시 시작합니다(모두 개발 중 폴링으로 생성된 것이며 실제 사용 기록이 아닙니다).

**결함 9 상세** — 뉴스 소스를 6개로 늘린 직후 QA 전체가 타임아웃됐습니다.
`feedparser.parse(url)` 이 URL을 직접 받으면 내부적으로 `urllib` 으로 받아오는데
**타임아웃 설정이 없습니다.** 응답이 느린 피드 하나가 전체 요청을 붙잡았습니다.
수정 후 미국 예측 응답이 7.4초로 정상화됐습니다.

**결함 8 상세** — 최초 측정에서 `/api/quote`가 2.08초로 나와 성능 미달 판정을
받았습니다. 그러나 아무 계산도 하지 않는 `/api/stats`조차 2.01초였습니다.
curl로 분해하니 총 0.213초 중 **0.210초가 TCP 연결 수립**이었습니다.
브라우저는 연결을 재사용하므로 실제 사용 조건과 달랐던 것입니다.

| 측정 방식 | `/stats` | `/quote` |
|---|---|---|
| urllib (매번 새 연결) | 2.045s | 2.076s |
| requests 세션 재사용 | 0.003s | **0.052s** |

---

## 4. 성능

| 엔드포인트 | 응답 | 비고 |
|---|---|---|
| `/api/quote` | **0.052s** | 4초 폴링용 — 여유 충분 |
| `/api/predict` (코스피) | 3.36s | 뉴스·커뮤니티·분봉 수집 포함 |
| `/api/predict` (미국) | 6.54s | Yahoo RSS + StockTwits |
| `/api/chart` | 2.0~2.5s | 주기 무관 |

`/quote`가 빠른 이유는 `/predict`가 받아둔 무거운 데이터(일봉·뉴스·커뮤니티)를
3분 TTL 캐시로 재사용하고, 가격만 실시간으로 조회하기 때문입니다.

---

## 5. 데이터 소스 도달성 (실측)

| 소스 | 상태 | 실측 |
|---|---|---|
| KRX 상장법인 목록 | ✅ | 2,746종목 |
| 네이버 분/일/주/월/년봉 | ✅ | 년봉 1990년~ |
| 토스증권 실시간 체결가 | ✅ | |
| 네이버 투자자별 매매동향 | ✅ | 개인/외국인/기관 |
| 네이버 종목뉴스 | ✅ | 20건 |
| 네이버 종목토론방 | ✅ | 40건 |
| StockTwits (미국) | ✅ | 30건, **작성자 Bullish/Bearish 태그** |
| 한국경제·매일경제·연합뉴스 RSS | ✅ | 종목 언급 기사만 필터 |
| MarketWatch·CNBC·Investing RSS | ✅ | |
| 머니투데이·아시아경제 RSS | ✅ | 국내 추가 |
| Yahoo Finance·Seeking Alpha·Nasdaq RSS | ✅ | 해외 추가 |
| **TradingView 아이디어** | ✅ | 키 불필요 · **국내(KRX:) + 미국 모두 커버** |
| **Hacker News (Algolia)** | ✅ | 키 불필요 · 국내 종목은 영문명으로 검색 |
| Reddit | ⚠️ 제한적 | RSS는 **429 잦음** → OAuth2 키 넣으면 해소 |
| 토스·팍스넷·카카오페이증권 커뮤니티 | ✅ 확장 경유 | 서버 직접은 401/404/연결거부 → 스크랩으로 우회 |
| **토스증권 Open API** | ⚠️ 미검증 | 스펙 확인 완료(OpenAPI 3.0), 키 미발급 |
| 한국투자증권 KIS | ⚠️ 미검증 | 키 미설정 |
| 공공데이터포털 · KRX Marketplace · 네이버 검색 | ⚠️ 미검증 | 엔드포인트 존재는 401 응답으로 확인, 키 미발급 |
| 카카오페이증권 API | ❌ 없음 | 개발자센터는 결제 전용 |
| 팍스넷 API | ❌ 없음 | 개발자 포털 도메인 부재 |

### 공식 API 실존 확인 (키 없이 401/400 응답으로 검증)

| 대상 | 응답 | 판정 |
|---|---|---|
| `openapi.tossinvest.com/openapi-docs/latest/openapi.json` | 200 (309KB 스펙) | ✅ 실존 — 32개 엔드포인트 확인 |
| `reddit.com/api/v1/access_token` | 401 | ✅ 실존 (OAuth2) |
| `apis.data.go.kr/.../getStockPriceInfo` | 401 Unauthorized | ✅ 실존 |
| `openapi.naver.com/v1/search/news.json` | 401 "Not Exist Client ID" | ✅ 실존 |
| `openapi.krx.co.kr` | 200 (Data Marketplace) | ✅ 실존 |
| `developers.kakaopay.com` | 200 (결제 문서만) | ❌ 증권 API 없음 |
| `developers.paxnet.co.kr` | DNS 실패 | ❌ 부재 |

---

## 6. 미해결 항목 및 한계

### 6.0 스크랩 기능의 한계

- **확장프로그램 셀렉터는 추정입니다.** 토스·팍스넷·카카오페이증권의 실제 DOM 구조를
  제가 브라우저로 확인하지 못해 일반적인 패턴(`[class*="comment"]` 등)으로 작성했습니다.
  실패해도 조용히 넘어가도록 만들었으므로 브라우징에 지장은 없지만, **수집이 0건이면
  해당 사이트에서 개발자도구로 확인해 셀렉터를 고쳐야 합니다.**
- **사용자가 페이지를 열어야만 수집됩니다.** 자동 순회는 하지 않습니다(의도된 설계).
- Reddit은 429가 잦아 실질적으로 간헐적으로만 수집됩니다.

### 6.1 검증하지 못한 것

- **한국투자증권 KIS 어댑터** — 공식 문서 스펙(tr_id, 필드명)대로 구현했으나
  실제 계정 키로 응답을 확인하지 못했습니다. 키 발급 후
  `python -m data_sources.kis_client` 로 먼저 자체 점검이 필요합니다.
- **예측 적중률** — 표본 0건. 이 시스템의 핵심 주장(가중치 자가학습)은
  `python main.py close` 를 매일 돌려 20건 이상 쌓이기 전까지 **검증되지 않았습니다.**
- **장중 실시간 동작** — 모든 테스트가 장 마감 시간대에 수행되었습니다.
  4초 폴링, 초봉 누적, 적중 판정의 실시간 갱신은 개장 후 재확인이 필요합니다.

### 6.2 설계상의 한계 (수정 대상이 아님)

- **시평선 파라미터는 판단값입니다.** `intraday`/`conf` 수치는 이론적으로 유도된
  상수가 아니라 설계자가 정한 값입니다. backtest로 시평선별 적중률이 쌓이면
  그 결과로 교체하는 것이 옳습니다.
- **공휴일 달력** — 내장 시간표만 쓰면 공휴일에 "장중"으로 표시됩니다.
  **토스 Open API 키를 넣으면 해소됩니다**(공식 캘린더 사용). 대시보드에 판정 출처를
  표시하므로 어느 경로인지 확인할 수 있습니다.
- **초봉은 과거 데이터가 없습니다.** 어떤 무료 API도 초 단위를 제공하지 않아
  접속 후 수신분만 누적합니다.
- **감성 사전은 문맥을 이해하지 못합니다.** "End of the AI Trade" 같은 표현의
  함의를 파악하지 못하며, 표본 확신도 감쇠로 영향을 제한하고 있을 뿐입니다.
- **레버리지·인버스 ETF는 예측 대상으로 부적합합니다.** 일일 리밸런싱 구조상
  기초지수 방향을 맞춰도 손실이 날 수 있습니다. 화면에 경고를 표시합니다.

### 6.3 이 시스템이 하지 않는 것

호가창·체결 강도·프로그램 매매·공매도 잔고·재무제표·거시지표를 보지 않습니다.
기술적 지표·뉴스 헤드라인·커뮤니티 여론 세 가지만 봅니다.

---

## 7. 판정

**조건부 합격.** 구현된 기능은 설계대로 동작하며 수치 검증을 통과했습니다.

다만 **예측 정확도 자체는 아직 검증되지 않았습니다.** 적중률 표본이 0건이기
때문입니다. 정확도에 대한 유일하게 정직한 검증은 매일 예측을 쌓고
`python main.py close` 로 채점해 누적 적중률을 확인하는 것입니다.
그 전까지 이 시스템의 확률은 **정교하게 계산된 가설**이지 검증된 예측이 아닙니다.

투자 판단의 단독 근거로 사용해서는 안 됩니다.
